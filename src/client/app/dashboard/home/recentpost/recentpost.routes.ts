import { Route } from '@angular/router';
import { RecentPostComponent } from './index';

export const HomeRoutes: Route[] = [
  	{
    	path: 'recentpost',
    	component: RecentPostComponent
  	}
];
